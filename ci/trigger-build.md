# CI build trigger

This file intentionally records the CI validation point for the current source payload and installer toolchain.

The authoritative build inputs remain `winamp/winamp_latest_installer.exe`, `installer/Install-Wulf.nsi`, and the scripts under `scripts/`.
